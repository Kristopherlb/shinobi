import { ReactNode } from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface PanelProps {\n  title: string;\n  rightActions?: ReactNode;\n  children: ReactNode;\n  className?: string;\n}\n\nexport function Panel({ title, rightActions, children, className }: PanelProps) {\n  return (\n    <div className={cn('bg-card border border-card-border rounded-lg overflow-hidden', className)}>\n      <div className=\"flex items-center justify-between p-4 border-b border-card-border bg-card\">\n        <h2 className=\"text-lg font-semibold text-card-foreground\">{title}</h2>\n        {rightActions && (\n          <div className=\"flex items-center gap-2\">\n            {rightActions}\n          </div>\n        )}\n      </div>\n      <div className=\"p-4\">\n        {children}\n      </div>\n    </div>\n  );\n}