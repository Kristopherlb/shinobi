{
  "name": "queue-processor-service-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd .. && tsc && cp src/package.json dist/ && cd dist && npm install --production"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.637.0",
    "@aws-sdk/client-s3": "^3.637.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0"
  }
}

