# Observability Configuration

This folder contains observability configuration for the EFS Filesystem component:

- `dashboards/` - CloudWatch/Grafana dashboard definitions
- `alarms/` - CloudWatch alarm configurations

These files are generated during component synthesis and used by the platform's observability service.

