{
  "name": "@shinobi/components-efs-filesystem",
  "sourceRoot": "packages/components/efs-filesystem/src",
  "tags": [
    "scope:shinobi",
    "type:component",
    "infra:aws"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/components/efs-filesystem/tsconfig.json",
        "cwd": "{workspaceRoot}"
      },
      "outputs": [
        "{workspaceRoot}/dist/packages/components/efs-filesystem"
      ]
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "packages/components/efs-filesystem/vitest.config.ts",
        "passWithNoTests": true
      },
      "outputs": [
        "{projectRoot}/coverage"
      ]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/components/efs-filesystem --ext .ts",
        "cwd": "{projectRoot}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p packages/components/efs-filesystem/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
