{
  "name": "@shinobi/components-lambda-worker",
  "sourceRoot": "packages/components/lambda-worker/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/packages/components/lambda-worker"
      ],
      "options": {
        "command": "pnpm exec tsc -b packages/components/lambda-worker/tsconfig.build.json"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "packages/components/lambda-worker/vitest.config.ts",
        "passWithNoTests": true
      },
      "outputs": [
        "{projectRoot}/coverage"
      ]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rimraf dist/packages/components/lambda-worker"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/components/lambda-worker/tsconfig.lib.json --noEmit"
      }
    }
  },
  "tags": [
    "scope:shinobi",
    "type:component",
    "infra:aws"
  ]
}

