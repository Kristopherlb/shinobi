apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: network-rules-stack
  title: Network Rules Stack
  description: Applies cross-stack security group rules from SSM Parameter Store to target security groups.
  tags:
    - component
    - aws
    - networking
    - security-groups
    - cross-stack
    - ssm
  annotations:
    platform.shinobi.dev/component-type: network-rules-stack
    github.com/project-slug: project42/shinobi
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: production
  owner: group:default/platform-engineering
  system: system:default/shinobi
  metadata:
    platform:
      componentType: network-rules-stack
      category: networking
      description: Infrastructure component that reads cross-stack security group rule specifications from SSM Parameter Store and applies them to target security groups at deployment time.
      capabilities: []
      awsServices:
        - ec2
        - ssm
        - lambda
        - logs
        - cloudwatch
      tags:
        required:
          - networking
          - security-groups
          - cross-stack
          - infrastructure
      documentation:
        readme: packages/components/network-rules-stack/README.md
        schema: packages/components/network-rules-stack/Config.schema.json




