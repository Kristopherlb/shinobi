{
  "name": "@shinobi/components-rds-postgres",
  "sourceRoot": "packages/components/rds-postgres",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec tsc -b packages/components/rds-postgres/tsconfig.build.json"
      },
      "outputs": [
        "{workspaceRoot}/dist/packages/components/rds-postgres"
      ]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/components/rds-postgres/tsconfig.json --noEmit"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rimraf dist/packages/components/rds-postgres"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": [
        "{projectRoot}/coverage"
      ],
      "options": {
        "config": "packages/components/rds-postgres/vitest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --ext .ts,.tsx"
      }
    }
  },
  "tags": [
    "scope:shinobi",
    "type:component",
    "infra:aws"
  ]
}
