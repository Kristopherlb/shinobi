{
  "name": "@platform/components-lambda-api",
  "sourceRoot": "packages/components/lambda-api/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest -c ../../../../jest.preset.ts --roots=./src"
      }
    }
  }
}