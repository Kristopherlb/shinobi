# Scripts Directory

This directory contains executable scripts that agents can run when using this skill.

**Progressive Disclosure**: Scripts are loaded only when explicitly needed, keeping the initial skill context small.

Scripts should:
- Be self-contained or clearly document dependencies
- Include helpful error messages
- Handle edge cases gracefully


